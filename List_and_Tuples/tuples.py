#Tuples are used to store multiple items in a single variable.
#Tuple is one of 4 built-in data types in Python used to store collections of data
#Tuple is created using parentheses ()
#Tuple is ordered, unchangeable, and allows duplicate values
marks = (90, 80, 70, 60, 50)
print(marks)
print(type(marks))
print(marks[0])  # access tuple item using index
print(marks.index(1)) # access tuple item using index

student =("vinay", 25, 5.8, True) # tuple can store different data types
print(student)